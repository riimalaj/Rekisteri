generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Huolto {
  id    Int     @id @default(autoincrement())
  kohde  String
  huolto String
  pvm DateTime
}